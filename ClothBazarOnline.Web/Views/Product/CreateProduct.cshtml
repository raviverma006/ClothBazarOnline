@model OProduct

@{
    ViewBag.Title = "CreateProduct";
}

<h2>CreateProduct</h2>

@using (Html.BeginForm("CreateProduct", "Product"))
{

<div class="form-group">
    @Html.LabelFor(Model => Model.Name, new { @class = "label label-primary" })
    @Html.TextBoxFor(Model => Model.Name, new { @class = "Form-control" })
    <br />
    @Html.LabelFor(Model => Model.Price, new { @class = "label label-primary" })
    @Html.TextBoxFor(Model => Model.Price, new { @class = "Form-control" })

    <br />
    @*@Html.LabelFor(Model=> Model.OCategories , new { @class ="label label-primary"})
    @Html.DropDownList("Category", new SelectList)*@

    <input type="submit" value="Save" class="btn btn-primary" />

</div>
}
